import NavbarComponent from '@/components/ui/navbar';

const HeroComponent = () => {
  return (
    <section className="">
      <div className="container">
        <NavbarComponent />
      </div>
    </section>
  );
};

export default HeroComponent;